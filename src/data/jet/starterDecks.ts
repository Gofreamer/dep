import type { DeckDef } from '../fixtures/nexo/decks';

/**
 * STARTER DECKS JET — aguardando o roster importado (Parte 18).
 *
 * Estrutura pronta: cada deck tem identidade própria baseada nas equipes
 * oficiais com roster suficiente (ex.: KOF 12 → sinergia/pressão/energia;
 * Asgard → controle/defesa/progressão; Morning Star → versátil/liderança).
 * A composição real (agentes + energias + técnicas) só é escrita quando os
 * agentes curados estiverem registrados — decks JAMais listam agentes
 * não confirmados pela fonte.
 */
export const JET_STARTER_DECKS: DeckDef[] = [];
